version: '3.8'
services:
  postgres:
    image: postgres:15
    restart: unless-stopped
    environment:
      POSTGRES_USER: vpnos
      POSTGRES_PASSWORD: vpnos_pass
      POSTGRES_DB: vpnos
    volumes:
      - pgdata:/var/lib/postgresql/data
    ports:
      - "5432:5432"
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U vpnos -d vpnos"]
      interval: 5s
      timeout: 5s
      retries: 10

  backend:
    build:
      context: ./backend
    environment:
      - DATABASE_URL=postgres://vpnos:vpnos_pass@postgres:5432/vpnos?sslmode=disable
      - BACKEND_PORT=8080
      - ADMIN_API_KEY=CHANGE_ME
      - XUI_BASE_URL=${XUI_BASE_URL:-http://x-ui:2053}
      - XUI_USERNAME=${XUI_USERNAME:-admin}
      - XUI_PASSWORD=${XUI_PASSWORD:-CHANGE_ME}
      - XUI_INBOUND_ID=${XUI_INBOUND_ID:-1}
    ports:
      - "8080:8080"
    depends_on:
      postgres:
        condition: service_healthy
    volumes:
      - ./backend:/src
    restart: unless-stopped

volumes:
  pgdata:
